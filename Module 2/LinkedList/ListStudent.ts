import {NodeScore} from "./Node";
class ScoreList<T> {
    head:NodeScore<T> | null;
    tail: NodeScore<T> |null;
    size: number;
    constructor(head:NodeScore<T>,tail:NodeScore<T>, size: number) {
        this.head = head;
        this.tail = NodeScore<T>;
        this.size: = number;
    }
}